import 'package:flutter/material.dart';

class StartupRegistrationPage extends StatelessWidget {
  const StartupRegistrationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Register Startup')),
      body: const Center(child: Text('Startup Registration — coming soon')),
    );
  }
}
